@model IEnumerable<KlubSosnowy.Models.ApplicationUser>

@{
    ViewBag.Title = "Index";
}

<h2>Users Roles</h2>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Roles)
        </th>
        @*<th></th>*@
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
                        <td>
                @Html.DisplayFor(modelItem => item.Roles.Count)
            </td>
            @*<td> tutaj bedzie dodawanie i odejmowanie roli uzytkownika
                @Html.ActionLink("Edit", "Edit", new { id = item.IdPotrawy }) |
                @Html.ActionLink("EdytujPotrawe", "EdytujPotrawe", new { id = item.IdPotrawy }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.IdPotrawy })
            </td>*@
        </tr>
    }

</table>